[package]
name = "scheduler"
version = "0.1.0"
edition = "2024"
description = "Model scheduling and turn orchestration for lite-code"
license = "MIT"

[dependencies]
async-trait = { workspace = true }
futures = { workspace = true }
openai-rs = { path = "../openai-rs" }
protocol = { path = "../protocol" }
serde_json = { workspace = true }
session-kernel = { path = "../session-kernel" }
