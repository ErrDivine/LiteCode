[package]
name = "session-kernel"
version = "0.1.0"
edition = "2024"
description = "Live session runtime kernel for lite-code"

[dependencies]
anyhow = "1"
async-trait = { workspace = true }
protocol = { path = "../protocol" }
rollout = { path = "../rollout" }
thread-store = { path = "../thread-store" }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
