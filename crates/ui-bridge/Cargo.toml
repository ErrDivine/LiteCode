[package]
name = "ui-bridge"
version = "0.1.0"
edition = "2024"
description = "CLI and web event adaptation for lite-code"

[dependencies]
protocol = { path = "../protocol" }
serde = { workspace = true }
serde_json = { workspace = true }
status = { path = "../status" }
