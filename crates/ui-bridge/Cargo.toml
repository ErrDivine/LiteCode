[package]
name = "ui-bridge"
version = "0.1.0"
edition = "2024"
description = "CLI, web, and VSCode event adaptation for lite-code"
license = "MIT"

[dependencies]
protocol = { path = "../protocol" }
pave-router = { path = "../pave-router" }
serde = { workspace = true }
serde_json = { workspace = true }
status = { path = "../status" }
