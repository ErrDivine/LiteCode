# Repository Mapping Flow

List top-level directories, inspect package manifests, then search for the target symbol or route. Read module declarations before leaf files when the language has explicit module wiring.

When explaining, cite the files that establish ownership and runtime flow. Keep the summary tied to discovered code rather than inferred naming.
